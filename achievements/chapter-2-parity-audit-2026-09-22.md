# Weru 97 Chapter 2 — Parity Audit Achievement

Date: 2026-09-22

## Completed in this slice

- Audited all 12 requested Stitch IDs and the local boot source one by one.
- Recorded extraction scope so duplicate Stitch desktop wrappers are not copied into application windows.
- Recorded source dimensions, target components, missing-source status, and intentional full-width behavior in `plans/weru97-chapter-2-parity-audit-2026-09-22.md`.
- Fixed the central MS-DOS `crash → Weru 97 recovery screen → desktop` path and verified it in the live browser.
- Added regression tests for persisted window migration/clamping, terminal effects, and the 12-screen manifest.
- Replaced remaining generic application icon usage in system surfaces with local pixel assets where an asset exists.
- Added classic-path CSS overrides preventing modern rounded cards, glass effects, and soft shadows from leaking into Win97 windows.
- Verified TypeScript, lint, 9 test files / 21 tests, and production build.
- Verified the full-width Bliss shell, larger taskbar, Start → Programs application list, Internet Explorer surface, and Media Player surface in the live browser.
- Added the missing My Pictures desktop shortcut with a versioned persisted-state migration, repaired legacy Recycle Bin/MS-DOS positions, and verified the migrated layout after reload.
- Verified My Pictures opens the classic Paint window and that the shared title-bar close control removes it cleanly.
- Compared the supplied Stitch desktop source through a local read-only HTTP view with the live runtime, then aligned the canonical icon flow, hid the non-source MS-DOS desktop shortcut, and added the source quick-launch strip.
- Compared the supplied Paint source with the live runtime and replaced the generic Paint placeholder with its cyber-engine wireframe, metrics, memo, 28-color palette, status strip, and real drawing overlay.
- Rebuilt the boot overlay from the supplied source structure: full-screen Award BIOS text, Energy Star header, starting state, cloud splash, four-color flag, segmented loader, Weru 97 branding, skip behavior, and clean handoff to the full-width desktop. Captured BIOS and splash stages live on localhost:3001.
- Rebuilt the CD Player surface from the supplied source scope with LCD modes, transport controls, track selectors, playlist, volume/balance, toggles, graphic equalizer controls, and no-disc behavior; live accessibility verification confirmed all controls and taskbar close state.
- Rebuilt the Media Player surface from the supplied source scope with the 3D wireframe viewport, OSD metadata, playlist file details, transport/LCD controls, compact player, codec notice, equalizer animation, real media element path, and explicit no-media fallback. Captured the complete surface live on localhost:3001; playback remains conditional on supplied media assets.
- Tightened Explorer and Notepad against the supplied source: Explorer now exposes the classic labeled toolbar and Address row while retaining filesystem operations; live screenshots verified Explorer and read-only Notepad chrome, content area, status rows, and taskbar state.
- Fixed and verified independent Explorer instances: My Documents and Projects now honor their explicit window ids instead of reusing the first Explorer. A deterministic classic cascade exposes the second window like the Stitch reference. Live QA confirmed separate rectangles, focus/z-order, taskbar buttons, and closing Projects without closing My Documents.
- Aligned the shell menus to the Stitch reference: Start → Programs now includes the source-style Explorer/Notepad/WordPad-disabled/Paint/Media Player/CD Player/Calculator/Internet Explorer sequence, and the desktop context menu now has classic Arrange/Line up/Refresh/Paste-disabled/New/Properties rows with viewport-safe placement.
- Tightened the media-window parity pass: IE now exposes the full Stitch toolbar sequence and functional browsing history, while CD Player now preserves the reference's wide main deck plus adjacent Graphic Equalizer proportions instead of clipping the track list.
- Verified Paint against its supplied Stitch screen: the measured 840×478 shell presents the matching wireframe canvas, geometry metrics, memo, toolbox, palette, zoom, status strip, and pixel rendering; the later supplied-image path is recorded below.
- Measured and corrected Calculator/Minesweeper placement and sizing from the supplied HTML: Calculator `278×265` at `(94,38)` and Minesweeper `242×276` at `(356,46)`, with the compact Calculator grid retained inside the measured rectangle.
- Corrected Paint to the measured `840×478` Stitch window height so its palette and status strip remain above the full-width Weru taskbar instead of being clipped.
- Added a shared window lifecycle regression test covering clamped move/resize, maximize/restore, minimize/restore, focus, and close; the suite now passes 21 tests across 9 files.
- Rechecked Internet Explorer after the accessibility refinement: History is now a real toggle button with expanded/collapsed state and a visible browsing-history entry, and the one-by-one comparison ledger now records all ten local Stitch sources as inspected.
- Reran the final gates after that change: TypeScript, lint, 9 test files / 21 tests, and the production build all passed; only the existing stale Browserslist data notice remains.
- Hardened Paint pointer behavior with pointer-capture start/end guards plus eraser and fill actions. Added shared reduced-motion handling and explicit media error/end-state reporting; the live no-media Play path now reports `No media loaded` instead of claiming playback.
- Removed the remaining modern content surfaces from the active classic router: legacy portfolio/settings/project/photo/terminal routes now resolve to Win97 Explorer, Notepad, Paint, MS-DOS, Control Panel, or the new classic Contact form. Preserved the supplied icon-sheet reference at `public/assets/win97/reference/windows_97_simulation_icons.jpg` for QA without rendering the sheet as production UI.
- Completed responsive viewport evidence at wide, laptop, tablet, and narrow requested sizes. The shell filled each measured viewport, the 46px taskbar stayed usable, Bliss remained the outer surface, and no document-level overflow was introduced; exact per-source pixel diff and full touch coverage remain intentionally open.
- Routed the supplied `windows_97_simulation_icons.jpg` sheet into the virtual Screenshots folder and verified Explorer → Paint opens the real image inside the classic Paint surface. Added a sky-blue document fallback so viewport scaling cannot reveal a legacy teal surround.
- Re-ran the final gates after the image route and fallback change: TypeScript, lint, 9 test files / 21 tests, and production build all passed; only the stale Browserslist data notice remains.
- Split the Paint evidence honestly in the active ledger: image viewing was marked `[x]`, while drawing remained partial until the dedicated live stroke capture in the next verification slice.
- Fixed persisted media-window rehydration: Paint now resolves its asset from the stored filesystem `fileId`, so a reload no longer falls back to the cyber-engine placeholder. Verified the supplied image after a full boot handoff plus live pencil, eraser, and fill interactions. The active Chapter 2 ledger is now 76 verified tasks, 6 evidence-partial tasks, and 0 unchecked tasks.
- Added isolated engine and contract tests for boot skip, calculator, Minesweeper, and the visitor fallback; the final gate now passes with 13 test files and 27 tests. The active Chapter 2 ledger is now 77 verified tasks, 5 evidence-partial tasks, and 0 unchecked tasks.
- Fetched the System Dialogs Stitch ZIP from project `2030522406260678629`, preserved its raw HTML, preview, design notes, and ZIP archive, and added the source to the manifest as available with visual comparison still partial. The remaining independent source gaps are the alternate dual-Explorer variant and the second system-dialog variant.
- Because the new source is available but its one-by-one comparison is not yet complete, the active Chapter 2 ledger is now 76 verified tasks, 6 evidence-partial tasks, and 0 unchecked tasks. This downgrade is intentional: the tracker does not count an acquired source as visually verified.
- Implemented the source-defined System Properties surface with four tabs, retro computer/specification panel, registration/workstation details, and segmented performance gauges; increased its window height to preserve the Stitch composition and added the missing Cancel action. The active ledger is now 75 verified tasks, 7 evidence-partial tasks, and 0 unchecked tasks because final dialog pixel comparison and Control Panel source parity remain open.
- Reran the complete validation gates after the dialog update: TypeScript and lint passed, 13 test files / 28 tests passed, and the production build passed. The only build notice is the existing stale Browserslist data warning.
- Replaced the Recycle Bin's remaining modern utility/card markup with explicit Win97 structure: classic toolbar, empty state, deleted-file rows, restore/delete actions, and a beveled confirmation dialog. The related visual-comparison item remains partial until the Stitch dialog composition and Control Panel are compared directly.
- Restored IE4 as a dedicated active implementation after removing its unreferenced legacy browser wrapper. Live double-click launch, source-derived toolbar/address/history surface, and title-bar close were verified on localhost:3001; TypeScript, lint, 13 test files / 28 tests, and production build all pass.
- Corrected the live System Properties general tab after comparing it against the fetched dialog image: the inner body now stays gray, the specification block is inset in the classic surface, and the resource gauges sit in a labeled performance group. Validation remains clean; exact pixel comparison and Control Panel source parity remain intentionally partial.
- Live-opened Control Panel from Start → Programs and verified its four functional classic settings actions after the icon-grid restyle. It is recorded as implemented behavior, not as Stitch pixel parity, because Stitch did not provide an independent Control Panel source.
- Recovered the alternate Dual Explorer Stitch export from project `2030522406260678629`, preserved its raw HTML, preview, and design notes under `Stitch Designs/`, and updated the manifest to distinguish source availability from visual parity. The source was inspected as a My Documents window extraction; its embedded desktop wrapper is intentionally discarded, and the live Explorer lifecycle is verified while exact source-specific pixel comparison remains partial.
- Reconciled the Chapter 2 ledger with the recovered source: twelve local Stitch HTML references are now recorded, only the second System Dialogs ID remains source-missing, and the manifest regression suite now passes with 13 test files / 29 tests. TypeScript, lint, and production build also pass; the build retains only the existing stale Browserslist notice.
- Localized the remaining user-visible virtual filesystem metadata label from Windows 97 to Weru 97 while preserving the supplied image filename and source archive provenance.

## Honest remaining work

Exact pixel parity, full pointer/touch coverage, the one remaining independent Stitch artifact, and real media playback still require their corresponding source/evidence. They remain partial in the Chapter 2 tracker rather than being falsely presented as complete.
