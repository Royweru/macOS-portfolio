// The terminal surface lives at the repository-level windows boundary while
// the rest of the legacy window components remain under src/windows.
export { default } from '../../windows/TerminalContent';
